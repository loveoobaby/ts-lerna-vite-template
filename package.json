{
  "name": "root",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "scripts": {
    "build": "lerna exec pnpm build --no-private --no-bail",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "eslint": "^8.1.0",
    "lerna": "7.1.5",
    "prettier": "^2.4.1"
  },
  "dependencies": {
    "@babel/core": "^7.23.2"
  }
}
